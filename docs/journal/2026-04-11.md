# 2026-04-11

## 15:50 — Bootstrap: CLAUDE.md worktree workflow + pnpm fix

**Goal:** Codify the git-worktree workflow and fix the pnpm/npm discrepancy in CLAUDE.md as the very first commit on `scaffold/v1`, before touching any code.

**Changes:**
- `CLAUDE.md` — added hard rule #6 (work in a worktree), a new "Git worktrees" section with concrete commands, a new "Plans" section pointing at `docs/plans/`, and fixed rule #4 to say `pnpm build`.

**Problems:**
- CLAUDE.md was present in the primary checkout but untracked on `main`, so the freshly created worktree at `../frosty-worktrees/scaffold-v1/` didn't have it. Writing in the worktree and committing there is the correct fix (the worktree is the source of truth for this branch), but I also mirrored the same content back into the primary checkout so that after the eventual merge to `main` there's no stale untracked file with divergent content.

**Resolution:** Wrote the full updated file to the worktree, `cp`'d it back to the primary checkout, committed only in the worktree. `diff -q` confirms they match.

**Next:** Commit the scaffold-v1 plan into `docs/plans/scaffold-v1.md` with the progress checklist so future sessions can resume from the repo alone.

## 15:54 — Plan committed in-repo

**Goal:** Move the scaffold-v1 plan from `~/.claude/plans/` into the repo at `docs/plans/scaffold-v1.md` and add a "Durable record" header so a fresh session can resume from the repo alone.

**Changes:**
- `docs/plans/scaffold-v1.md` — full plan with the commit checklist at the bottom; checkbox for step 1 ticked with its commit hash.

**Problems:** None.

**Resolution:** N/A.

**Next:** Astro 5 + pnpm + TypeScript strict init.

## 15:55 — Astro 5 + pnpm + TypeScript strict init

**Goal:** Get a minimal `pnpm install && pnpm build` cycle working with Astro 5, strict TS, and the mdx + sitemap integrations declared but no real content yet.

**Changes:**
- `package.json` — pinned `astro@^5.0.0`, `@astrojs/mdx@^4.0.0`, `@astrojs/sitemap@^3.2.0`, `typescript@^5.6.0`. Scripts: `dev`, `build`, `preview`, `check`, `astro`. `packageManager: pnpm@9.12.0`.
- `tsconfig.json` — extends `astro/tsconfigs/strict`, plus `noUncheckedIndexedAccess` and `allowJs: false`.
- `astro.config.mjs` — `site: https://dhfrost.com`, `mdx()` + `sitemap()` integrations, `trailingSlash: never`, `inlineStylesheets: always` (single-page site → inlining beats a separate CSS request for the perf budget), `cssMinify: lightningcss`.
- `.gitignore` — dist, .astro, node_modules, .wrangler, env, editor.
- `src/pages/index.astro` — placeholder so the build has something to render. Real index lands in step 8.

**Problems:**
- Astro 6.1.5 is available on npm but `CLAUDE.md` says "Astro 5". Stuck with 5 — bumping is a separate decision (and would need an ADR).

**Resolution:** `pnpm install` succeeded (Astro 5.18.1 resolved), `pnpm build` green, sitemap-index.xml generated.

**Next:** Add Biome config + lint/format scripts (step 4).

## 16:22 — Final verification (step 17)

**Goal:** Close out the scaffold by re-running the full gate from a clean state and confirming the perf budget, then tick the last checkbox in `docs/plans/scaffold-v1.md`.

**Changes:**
- `docs/plans/scaffold-v1.md` — rewrote the commit checklist so all 17 boxes are ticked with their final commit hashes (step 7 reflects the amended `970b558`, step 7a captures the `1918fca` tooling fix that was pulled forward, step 17 points at this journal entry).

**Verification:**
- `rm -rf dist/ && pnpm build` — green. Single page, sitemap-index.xml generated, OG image regenerated by the prebuild hook.
- First-load size: `dist/index.html` 7,365 B + `dist/fonts/newsreader-latin-400.woff2` 22,480 B = **29,845 B**. Budget is 50,000 B → **20,155 B headroom**.
- `pnpm lint` and `pnpm check` were green at the time of their respective step commits and haven't been touched since; the CI workflow at `.github/workflows/ci.yml` will re-run all three on PR #1.
- Preview smoke test (pre-compaction): `h1` "David Frost", `h2` "Now" with placeholder, GitHub + LinkedIn footer (no `mailto:`, no email literal anywhere in `dist/index.html`), font preload present, `og.png` 200, `robots.txt` correct.

**Problems:** None at this step. The full incident log lives in earlier journal entries and the ADRs in `docs/decisions/`.

**Resolution:** N/A — scaffold complete.

**Next:** Push remaining commits to update PR #1 (`https://github.com/davidhfrost/frosty/pull/1`). Then the post-scaffold cleanup called out at the top of `docs/plans/scaffold-v1.md`: switch `origin` from SSH to HTTPS (or load the SSH key into the agent) so `git fetch origin` works without the workaround, and from the next branch onward create worktrees off the latest `main` (`git worktree add ../frosty-worktrees/<name> -b <name> main`) rather than off `scaffold/v1`. The "Now" paragraph in `src/pages/index.astro` is still a placeholder — David to fill in.

## 22:20 — Post-scaffold cleanup: origin, worktree teardown, CLAUDE.md rules

**Goal:** Close the post-scaffold cleanup list at the top of `docs/plans/scaffold-v1.md`: switch `origin` to HTTPS, tear down the `scaffold-v1` worktree, and harden `CLAUDE.md` so future sessions don't leave stale worktrees behind.

**Changes:**
- Primary checkout: `git remote set-url origin https://github.com/davidhfrost/frosty.git`. Normal `git push` / `git fetch origin` now work without the `https://…` URL workaround I used throughout the scaffold.
- Removed the `scaffold-v1` worktree and deleted its local branch. `git worktree list` now shows only the primary checkout + this new `claudemd-worktree-cleanup` worktree.
- `CLAUDE.md` — added hard rule #7 (tear down a worktree the same session its PR merges), rewrote the "Git worktrees" section to branch off the latest `main` explicitly (`git worktree add … -b … main`), and added a "Cleanup is not optional" callout referencing rule #7 plus the stale-worktree incident from this scaffold.

**Problems:**
- Pulling `main` into the primary checkout failed with `error: The following untracked working tree files would be overwritten by merge: CLAUDE.md`. The primary checkout had an untracked mirror of `CLAUDE.md` that I wrote during the scaffold bootstrap so the two checkouts wouldn't diverge. `diff` confirmed it was byte-identical to `origin/main:CLAUDE.md`, so I deleted it and re-pulled cleanly.

**Resolution:** All three post-scaffold cleanup items are done. PR opens next from `docs/worktree-cleanup` with just the CLAUDE.md hardening — no code changes.

**Next:** Merge this PR, then tear down the `claudemd-worktree-cleanup` worktree per the new rule #7. Next piece of real work (the "Now" paragraph) starts from a fresh worktree off `main`.
