# 2026-04-11

## 15:50 — Bootstrap: CLAUDE.md worktree workflow + pnpm fix

**Goal:** Codify the git-worktree workflow and fix the pnpm/npm discrepancy in CLAUDE.md as the very first commit on `scaffold/v1`, before touching any code.

**Changes:**
- `CLAUDE.md` — added hard rule #6 (work in a worktree), a new "Git worktrees" section with concrete commands, a new "Plans" section pointing at `docs/plans/`, and fixed rule #4 to say `pnpm build`.

**Problems:**
- CLAUDE.md was present in the primary checkout but untracked on `main`, so the freshly created worktree at `../frosty-worktrees/scaffold-v1/` didn't have it. Writing in the worktree and committing there is the correct fix (the worktree is the source of truth for this branch), but I also mirrored the same content back into the primary checkout so that after the eventual merge to `main` there's no stale untracked file with divergent content.

**Resolution:** Wrote the full updated file to the worktree, `cp`'d it back to the primary checkout, committed only in the worktree. `diff -q` confirms they match.

**Next:** Commit the scaffold-v1 plan into `docs/plans/scaffold-v1.md` with the progress checklist so future sessions can resume from the repo alone.
