# 2026-04-13

## 20:17 — Pin Node 22 locally via .nvmrc and engines

**Goal:** Close the drift gap between the Node version CI uses (22, set in `.github/workflows/ci.yml`) and what a local shell picks up.

**Changes:**
- `.nvmrc` — created with `22` so `nvm use` (or any `.nvmrc`-aware tool) picks the right version without consulting the CI file.
- `package.json` — added `engines.node: ">=22"` and `engines.pnpm: ">=9.12.0"` to document the known-good runtime.
- `docs/plans/infra-hygiene-pass.md` — plan file committed into repo (ephemeral `~/.claude/plans/` copy promoted to durable in-repo record).

**Problems:** None.

**Resolution:** `pnpm lint` and `pnpm build` green. First-load size unchanged.

**Next:** PR 2 — add `src/pages/404.astro`.

## 20:22 — Add 404 page

**Goal:** Replace Cloudflare's default 404 chrome with a hand-written page that matches the site's layout and voice.

**Changes:**
- `src/pages/404.astro` — new page using `Base.astro` layout. H1 "Not found", one-sentence body with a link back to `/`. No em dashes, no emoji, terse.

**Problems:** None. Cloudflare Pages automatically serves `dist/404.html` for unknown paths — no config needed.

**Resolution:** `pnpm lint && pnpm build` green. First-load budget unchanged (404 is a separate file, not counted by the CI budget script). Dev server smoke-test at `/bogus` confirmed the layout, title, and "Go home" link render correctly.

**Next:** PR 3 — add raster favicon variants and web manifest.
