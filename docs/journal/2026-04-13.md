# 2026-04-13

## 20:22 — Add 404 page

**Goal:** Replace Cloudflare's default 404 chrome with a hand-written page that matches the site's layout and voice.

**Changes:**
- `src/pages/404.astro` — new page using `Base.astro` layout. H1 "Not found", one-sentence body with a link back to `/`. No em dashes, no emoji, terse.

**Problems:** None. Cloudflare Pages automatically serves `dist/404.html` for unknown paths — no config needed.

**Resolution:** `pnpm lint && pnpm build` green. First-load budget unchanged (404 is a separate file, not counted by the CI budget script). Dev server smoke-test at `/bogus` confirmed the layout, title, and "Go home" link render correctly.

**Next:** PR 3 — add raster favicon variants and web manifest.
