---
import Meta, { type ArticleMeta } from "../components/Meta.astro";
import { profile } from "../data/profile";
import "../styles/global.css";

interface Props {
  title: string;
  description: string;
  homepageSchema?: boolean;
  article?: ArticleMeta;
}

const { title, description, homepageSchema, article } = Astro.props;
---
<!doctype html>
<html lang="en">
  <head>
    <Meta title={title} description={description} homepageSchema={homepageSchema} article={article} />
    <link
      rel="preload"
      href="/fonts/newsreader-latin-400.woff2"
      as="font"
      type="font/woff2"
      crossorigin
    />
  </head>
  <body>
    <a href="#main" class="skip-link">Skip to content</a>
    <div class="page">
      <main id="main">
        <slot />
      </main>
      <footer>
        <a href="/about">About</a>
        {" · "}
        <a href={profile.social.github}>GitHub</a>
        {" · "}
        <a href={profile.social.linkedin}>LinkedIn</a>
        {" · "}
        <a href="/rss.xml">RSS</a>
      </footer>
    </div>
  </body>
</html>
